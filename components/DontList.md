# Don't List

A don't list presents discouraged actions or practices to avoid as a list of
items. Uses an unordered list with appropriate ARIA labelling.
# DontList

- `<ul role="list">` with `aria-label` defaulting to "Don't"
- Children are `<li>` discouraged items
