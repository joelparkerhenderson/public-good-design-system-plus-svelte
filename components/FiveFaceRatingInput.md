# Five Face Rating Input

A five-face rating input uses emoji faces for rating selection (from sad
to happy). Uses radio buttons for accessibility with proper grouping.
# FiveFaceRatingInput

- `<fieldset role="radiogroup">` with 5 radio buttons
- Default labels: Very bad, Bad, Okay, Good, Very good
- `$bindable()` value (1-5)
