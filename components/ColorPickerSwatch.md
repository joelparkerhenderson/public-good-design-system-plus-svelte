# Color Picker Swatch

A button representing a single predefined color choice. Uses `aria-pressed`
for selected state and exposes `data-color` for consumer CSS.
# ColorPickerSwatch

- Button with `aria-pressed` and inline background-color
