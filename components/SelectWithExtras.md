# Select With Extras

A select with extras wraps a native select element with optional before and after content areas, allowing consumers to add icons, labels, or other elements alongside the select.
