# Field

A field component wraps a form control with its label, description,
and error message. Provides proper labelling via htmlFor and
aria-describedby linkage.
# Field

- `<label>` linked to input via `for`
- Description via id for `aria-describedby`
- Error with `role="alert"`
- Required indicator `aria-hidden="true"`
