# Date Input

A date input provides a native HTML date input element with accessible
labelling via aria-label. It is a minimal wrapper for `<input type="date">`
with bindable value.
# DateInput

- `<input type="date">` with `aria-label`
- `$bindable()` value in YYYY-MM-DD format
- Supports min, max, required, disabled
