# Contents List

A table of contents navigation with ordered links to page sections.
# ContentsList

- `<nav>` with `aria-label` wrapping an `<ol>`
- Children rendered inside the ordered list
- Label prop required for accessibility
