# Datetime Local Input

A datetime-local input allows users to select both a date and time using
the native `<input type="datetime-local">` element. Provides accessible
labelling and bindable value.
# DatetimeLocalInput

- `<input type="datetime-local">` with `aria-label`
- `$bindable()` value
- Supports min, max, required, disabled
