# File Input

A file input wraps a native `<input type="file">` with accessible labelling.
Supports accept types, multiple file selection, and disabled state.
# FileInput

- `<input type="file">` with `aria-label`
- Supports accept, multiple, required, disabled
