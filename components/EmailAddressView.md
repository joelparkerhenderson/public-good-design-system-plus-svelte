# Email Address View

An email address view displays an email as a clickable mailto link with
accessible labelling.
# EmailAddressView

- `<a href="mailto:...">` link
- Displays email text and links to mailto
- Optional `aria-label` override
