# Tel Input

A tel input provides a telephone number input field using the native HTML
`<input type="tel">` element with accessible labelling via aria-label and
browser autofill support via `autocomplete="tel"`.
