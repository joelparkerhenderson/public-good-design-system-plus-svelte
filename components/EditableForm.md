# Editable Form

An editable form wraps an inline editable experience in a form element,
providing submit and cancel actions. Useful for editing records in place
with form submission semantics.
# EditableForm

- `<form>` shown only when editing
- `$bindable()` editing prop
- Submit handler with `preventDefault`
- Escape cancels via `oncancel` callback
