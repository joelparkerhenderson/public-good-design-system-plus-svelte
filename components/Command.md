# Command

A command palette provides a searchable list of actions. Users type to filter
and select commands via keyboard. Renders search input + listbox.
# Command

- Search input + listbox pattern, `$bindable()` value
