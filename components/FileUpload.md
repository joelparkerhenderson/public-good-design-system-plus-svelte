# File Upload

A file upload component provides a drop zone or button for uploading files.
Combines a file input with feedback about selected files.
# FileUpload

- Button triggers hidden `<input type="file">`
- `aria-live="polite"` status region shows file count
- `onchange` callback with FileList
