# Caption

## Implementation Notes

- Uses semantic `<figcaption>` element
- Designed for use within a `<figure>` parent
- Can be referenced via `id` and `aria-describedby`

## ARIA

- Implicit semantics from `<figcaption>`
