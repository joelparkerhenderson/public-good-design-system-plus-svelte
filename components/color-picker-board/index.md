# Color Picker Board

A 2D area selector for choosing colors by saturation (X) and brightness (Y).
Keyboard arrow keys navigate, Shift for large steps. Exposes `data-x` and
`data-y` for CSS cursor positioning.
