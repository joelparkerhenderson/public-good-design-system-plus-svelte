# Five Star Rating Input

A five-star rating input allows users to select a rating from 1 to 5.
Uses radio buttons for accessibility with proper grouping.
# FiveStarRatingInput

- `<fieldset role="radiogroup">` with 5 radio buttons
- `$bindable()` value (1-5)
- Each radio labeled "N star(s)"
