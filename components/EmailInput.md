# Email Input

An email input wraps a native `<input type="email">` with accessible labelling.
A simpler variant of EmailAddressInput for basic email collection.
# EmailInput

- `<input type="email">` with `aria-label`
- `$bindable()` value
- Supports required, disabled
