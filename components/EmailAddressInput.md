# Email Address Input

An email address input wraps a native `<input type="email">` with accessible
labelling. Used for collecting email addresses in forms.
# EmailAddressInput

- `<input type="email">` with `aria-label`
- `autocomplete="email"` for autofill
- Supports placeholder, required, disabled
