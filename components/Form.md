# Form

A form component wraps a native HTML form with accessible labelling,
submit handling with default prevention, and reset support.
# Form

- `<form>` with `aria-label`
- Prevents default submit
- Callbacks: `onsubmit`, `onreset`
