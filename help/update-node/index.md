# Update Node

Run:

```sh
npm update -g npm
```

For `nvm`:

```sh
nvm install latest
nvm alias default latest
```

For `brew`:

```sh
brew update
brew doctor
brew upgrade node
brew link --overwrite node
```
